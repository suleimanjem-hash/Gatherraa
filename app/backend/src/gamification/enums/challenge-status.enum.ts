export enum ChallengeStatus {
  DRAFT = 'draft',
  PUBLISHED = 'published',
  ACTIVE = 'active',
  COMPLETED = 'completed',
  EXPIRED = 'expired',
  CANCELLED = 'cancelled',
  NOT_STARTED = 'not_started',
  IN_PROGRESS = 'in_progress',
  FAILED = 'failed',
  PAUSED = 'paused',
}
